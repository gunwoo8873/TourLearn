# CodeQL
코드 검사는 CodeQL을 사용하여 GitHub 리포지토리의 코드를 분석하고 보안 취약성 및 코딩 오류를 찾는다. 모든 퍼블릭 리포지토리에 사용할 수 있으며 GHAS(`GitHub Advanced Security`)를 사용하여 설정한 조직이 소유한 프라이빗 리포지토리에 사용할 수 있다. 코드 검사에서 코드에 잠재적 취약성 또는 오류가 발견되면 GitHub가 해당 리포지토리의 보안 탭에 경고를 표시하고 트리거한 코드를 수정한 후 닫는다.  
코드 검색을 사용하여 코드에서 기존 문제에 대한 수정 사항을 찾고 심사하여 우선 순위를 지정할 수 있으며 검사를 통해 개발자는 새로운 문제가 발생하지 않도록 방지할 수 있다. 특정 날짜 및 시간에 대한 검사를 예약 할당하거나 푸시와 같은 특정 이벤트 트리거가 리포지토리에서 발생할 때 검사를 진행할 수 있다.

## 검사 안내
보안 확인을 자동화하기 위해 GitHub가 개발한 코드 분석 엔진이다. 코드를 분석하고 결과를 검사 경고로 표시하여 다음 세 가지 주요 방법이 존재한다.

* 기본 설정을 사용하여 리포지토리에서 코드 검색을 위한 CodeQL 분석을 신속하게 구성하낟. 언어, 실행 쿼리 도구, 수동 옵션 등을 사용하여 검사를 트리거하는 이벤트 선택을 처리한다.
* 고급 설정을 사용하여 워크플로를 리포지토리에 직접 추가하여 CodeQL-Actions을 사용하여 CLI에서 작업하는 사용자 지정 가능한 워크플로 파일이 생성된다.
* 외부 CI 시스템에서 직접 CodeQL을 사용하면 쿼리 결과가 GitHub 코드 검사 경고로 표시된다.

### 설정
리포지토리에 대한 쓰기 권한이 있다면 해당 리포지토리에 대한 코드 검사를 설정하거나 구성할 수 있다.

* 리포지토리의 기본 페이지로 이동
* 보안 탭 이동
* 코드 검색 설정을 선택(권한이 없는 사용자는 해당 조직 관리자나 소유자에게 고급 보안 지원을 요청)
* 설정 드롭다운에서 기본값을 선택
* 기본 옵션을 검토하고 필요한 경우 편집을 선택하여 CodeQL 실행 방법을 사용자 지정한다.
  * on:pull_request 및 on:push 트리거는 각 다른 용도로 유용한 코드 검색의 기본값
* 코드 검색을 활성화 준비가 되면 CodeQL 활성화 선택
  * 기본 CodeQL 분석 워크플로에서는 코드 검사가 보호된 분기로 변경 내용을 푸시하거나 기본 분기에 대한 끌어오기 요청을 실행할 때마다 코드를 분석하도록 구성된다. 푸시가 수행되면 해당 코드 검색이 자동으로 수행